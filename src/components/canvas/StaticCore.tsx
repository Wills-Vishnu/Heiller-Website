/**
 * REMOVED. See `SceneCanvas.tsx` in this directory for the full explanation —
 * the entire WebGL background layer has been taken out of the site. This file
 * was the reduced-motion / no-WebGL fallback for that layer and is no longer
 * needed now that there is no WebGL layer to fall back from.
 *
 *     rm -rf src/components/canvas
 */
export {};
