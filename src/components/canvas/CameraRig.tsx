/**
 * REMOVED. See `SceneCanvas.tsx` in this directory for the full explanation —
 * the entire WebGL background layer has been taken out of the site.
 *
 *     rm -rf src/components/canvas
 */
export {};
