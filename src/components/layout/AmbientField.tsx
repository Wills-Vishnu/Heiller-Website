/**
 * REMOVED.
 *
 * The drifting cobalt/coral gradient field that used to sit behind the whole
 * page. Removed at the user's request along with the rest of the animated
 * background (the WebGL scene in `components/canvas/`). The page background
 * is now the flat `--color-frost` set on `body` in `globals.css`.
 *
 * Please delete this file:
 *
 *     rm src/components/layout/AmbientField.tsx
 */
export {};
