/**
 * Reserved for ambient type declarations.
 *
 * Previously augmented the JSX namespace for `@react-three/fiber` intrinsic
 * elements (`<mesh>`, `<meshPhysicalMaterial>`, …). That package — along with
 * the rest of the WebGL background layer — has been removed from the project,
 * so this file is currently empty. Left in place, and still referenced from
 * `tsconfig.json`, so a future ambient declaration has an obvious home.
 */
export {};
